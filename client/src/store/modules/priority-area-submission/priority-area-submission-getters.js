export const activePriorityAreaSubmission = state => {
  return state.activePriorityAreaSubmission;
}

export const dirty = state => {
  return state.dirty;
}

export const priorityAreaSubmissions = state => {
  return state.priorityAreaSubmissions;
}

export const requestError = state => {
  return state.requestError;
}

export const requestStatus = state => {
  return state.requestStatus;
}

export const count = state => {
  return state.count;
}

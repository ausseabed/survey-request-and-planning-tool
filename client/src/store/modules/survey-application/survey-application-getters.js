export const surveyApplications = state => {
  return state.surveyApplications;
}

export const surveyApplicationGroups = state => {
  return state.surveyApplicationGroups;
}

export const selectedSurveyApplication = state => {
  return state.selectedSurveyApplication;
}

export const selectedSurveyApplicationGroup = state => {
  return state.selectedSurveyApplicationGroup;
}

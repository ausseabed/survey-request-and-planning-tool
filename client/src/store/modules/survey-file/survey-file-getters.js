export const files = state => {
  return state.files;
}

export const attachesTo = state => {
  return state.attachesTo;
}

export const attachesToId = state => {
  return state.attachesToId;
}


export const requestStatus = state => {
  return state.requestStatus;
}

export const requestError = state => {
  return state.requestError;
}

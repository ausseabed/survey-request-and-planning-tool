version: "3.4"
services:
  www:
    build:
      context: ./client
      args:
        - AUTH_HOST=https://staging.accounts.crcsi.com.au
    environment:
      - NODE_ENV=production
    ports:
      - '80:80'

  api:
    build:
      context: ./server
    env_file: ../config/server.conf
    environment:
      - ENVIRONMENT=production

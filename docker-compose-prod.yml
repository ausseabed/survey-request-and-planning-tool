version: "3.4"
services:
  www:
    build:
      context: ./client
      args:
        - AUTH_HOST=https://ausseabed-staging.auth.ap-southeast-2.amazoncognito.com
        - AUTH_CLIENT_ID=lf9uevj11r76cg9vai546po0f
    environment:
      - NODE_ENV=production
    ports:
      - '80:80'

  api:
    build:
      context: ./server
    env_file: ./server.conf
    environment:
      - ENVIRONMENT=production

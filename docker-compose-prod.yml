version: "3.4"
services:
  www:
    build:
      context: ./client
    environment:
      - AUTH_HOST=https://staging.accounts.crcsi.com.au
      - NODE_ENV=production
    ports:
      - '80:80'

  api:
    build:
      context: ./server
    env_file: ../config/server.conf
    environment:
      - ENVIRONMENT=production
